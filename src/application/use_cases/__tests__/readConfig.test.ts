// import { readConfig } from '../config/readConfig'; // Supprimé car non utilisé
describe('readConfig UseCase', () => {
  it('should read config', async () => {
    // TODO: implement test
    // Suggestion: use mocks for repositories
  });
});